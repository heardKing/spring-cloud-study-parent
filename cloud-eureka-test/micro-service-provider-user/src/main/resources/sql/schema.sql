DROP TABLE C_USER IF EXISTS;

CREATE TABLE C_USER(
  ID BIGINT GENERATED BY DEFAULT AS IDENTITY,
  USERNAME VARCHAR(40),
  NAME VARCHAR(20),
  AGE INT(3),
  BALANCE NUMBER(10,2),
  PRIMARY KEY(ID)
);
